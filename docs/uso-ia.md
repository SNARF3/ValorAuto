# Declaración de Uso de IA — ValorAuto

Conforme a la regla de integridad de `docs/team_charter.md` ("las decisiones de IA asistida se declaran en el PR" / "todo código generado debe revisarse, probarse y entenderse"), este documento consolida en un solo lugar dónde se usó IA generativa en el proyecto, para qué, y qué verificación humana falta o ya se hizo. Antes existía una declaración solo para `requirements/REQ-001-validado.md` (`docs/evidence/lab-claude-openspec.md`); este documento la extiende a todo el repositorio.

| Artefacto | Herramienta de IA | Qué se generó | Verificación humana |
|---|---|---|---|
| `requirements/REQ-001-validado.md` | Claude Code | Análisis de riesgos/ambigüedades del REQ-001 y propuesta de resolución para cada uno. | **Pendiente** — el equipo debe confirmar o ajustar las resoluciones (sección 3 del documento) antes de mover tareas de Build para este REQ. |
| `openspec/changes/endpoint-tasacion-mvp/` (proposal, spec, design, tasks) | Claude Code | Los 4 artefactos de la change de OpenSpec, a partir del REQ-001 analizado. | Validado automáticamente con `openspec validate` (pasa). Revisión de contenido por el equipo: pendiente, igual que el REQ-001 del que depende. |
| `.claude/skills/tsi-analizar-requerimiento/`, `.claude/skills/tsi-gate-desarrollo/` | Claude Code | Las dos skills locales del taller (detectar riesgos sobre un requerimiento ambiguo, auditar el gate antes de Build). | Probadas ejecutándolas sobre REQ-001 (ver `docs/evidence/lab-claude-openspec.md`). |
| `docs/architecture/C4-ValorAuto.md`, `docs/architecture/valorauto-c4-container.xml`, `docs/adr/0001` a `0008`, `docs/crisp-mlq.md`, `docs/risk-register.md`, `docs/evidence/baseline-modelo.md`, este mismo documento, la modularización de `src/model`/`src/pipeline` en componentes (`config.py`, `eda/*.py`, `training.py`, `evaluation.py`, `combinations.py`, `precalculo.py`) y los notebooks delgados en `notebooks/` | Claude (Cowork), sesión asistida por Marwin | Diagramas C4 (contexto y contenedores), ADRs de las decisiones de stack ya tomadas por el equipo (incluyendo ADR-0008 sobre la separación código/orquestación), mapeo a CRISP-ML(Q), consolidación del risk register disperso, documentación de la línea base ya ejecutada, y la extracción de la lógica de los notebooks originales de Samuel y Leonardo hacia módulos `.py` reutilizables con valores estáticos centralizados (resolviendo un conflicto real de hiperparámetros entre dos de esos notebooks), dejando notebooks delgados en `notebooks/` que solo orquestan esos módulos. | **Pendiente de revisión por Samuel y Leonardo** — las decisiones de stack documentadas en los ADRs ya estaban tomadas por el equipo (ver `docs/priorizacion_casos.md`, `docs/clickup_estructura.md`, `docs/contexto/contexto_proyecto.md`); lo que generó la IA es la redacción en formato ADR y el diagrama, y en el caso de la modularización, la reorganización de lógica que ya existía en sus notebooks (no lógica nueva de negocio). Se recomienda que los tres integrantes lean los ADRs y el código nuevo en `src/model`/`src/pipeline` antes de la defensa, porque el docente puede preguntar sobre cualquiera. El pipeline nuevo ya se ejecutó de punta a punta contra datos reales (2026-09-17, ver `docs/evidence/baseline-modelo.md`). |
| `LimpiezaValorAUTO.ipynb`, `XGBoost_Scikit_Learn.ipynb` (retirados del árbol, lógica absorbida en `src/model/cleaning.py` y `src/model/eda/*.py`, ver [ADR-0008](adr/0008-separacion-componentes-notebooks-delgados.md); git conserva su historial) | Sin declarar por el autor original (Samuel) | Notebooks de EDA/limpieza y comparación de modelos. | **Pendiente** — Samuel debe declarar aquí si usó algún asistente de IA (Colab AI, ChatGPT, Copilot) para escribir la lógica original, y qué verificó. Placeholder a completar por el equipo; sigue aplicando aunque el archivo ya no esté en el árbol, porque la lógica sigue siendo suya. |
| `train_baseline.ipynb`, `generate_combinations.ipynb` (retirados del árbol, lógica absorbida en `src/model/training.py`, `evaluation.py` y `src/pipeline/combinations.py`, `precalculo.py`, ver [ADR-0008](adr/0008-separacion-componentes-notebooks-delgados.md); git conserva su historial) | Sin declarar por el autor original (Leonardo) | Entrenamiento comparativo con MLflow y generación de combinaciones para precálculo. | **Pendiente** — Leonardo debe declarar aquí si usó algún asistente de IA para escribir la lógica original, y qué verificó (ej. que las métricas reportadas correspondan a una corrida real, que el modelo registrado sea el correcto). |
| Componente de producto: Google Gemini | N/A (parte del producto, no una herramienta de desarrollo) | Identificación de marca/modelo/año a partir de fotos de autos, en tiempo real dentro de la app. | Es el propio Gemini como servicio de visión (ver [ADR-0004](adr/0004-gemini-vision-vs-modelo-propio.md)), se declara aparte por ser un uso de IA distinto del uso de IA para programar. |

## Qué falta para cerrar esta declaración

Los renglones marcados "Pendiente" son responsabilidad de Samuel y Leonardo: cada uno debe indicar si usó IA generativa para escribir su notebook y qué revisó antes de darlo por bueno. Esto es un antipatrón de uso opaco de IA si se queda sin declarar antes de la defensa.
